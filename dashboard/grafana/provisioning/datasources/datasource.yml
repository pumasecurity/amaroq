apiVersion: 1
datasources:
- orgId: 1
  uid: AMAROQ
  version: 2
  name: InfluxDB
  type: influxdb
  access: proxy
  url: http://${DOCKER_INFLUXDB_INIT_HOST}:8086
  basicAuth: true
  isDefault: true
  jsonData:
    defaultBucket: $DOCKER_INFLUXDB_INIT_BUCKET
    httpMode: POST
    organization: $DOCKER_INFLUXDB_INIT_ORG
    version: Flux
  secureJsonData:
    token: $DOCKER_INFLUXDB_INIT_ADMIN_TOKEN